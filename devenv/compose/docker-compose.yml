version: "3.8"

services:
  backend:
    build:
      context: ../../
      dockerfile: devenv/docker/backend.Dockerfile
    ports:
      - "3001:3001"
    environment:
      - DATABASE_URL=file:/app/data/dev.db
    volumes:
      - backend_data:/app/data

volumes:
  backend_data:
